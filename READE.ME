Arquivo de inicialização do serviço do NGINX para ser colocado dentro do init.d ( /etc/init.d/nginx ).

Este arquivo irá funcionar para a instalação Source Release dentro de um CentOS 6.

O NGINX precisa estar instalado em /usr/local/nginx/sbin/nginx.
